# S09 Video Alternates Spike

Validates MP4 H.264 and Animated WebP export paths using synthetic BGRA frames and `ffmpeg.exe`.

Run:

```powershell
spike_s09_video_alternates.exe
```

Optional:

```powershell
spike_s09_video_alternates.exe C:\path\to\ffmpeg.exe artifacts\video
```
